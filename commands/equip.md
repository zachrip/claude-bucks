---
description: Change Claude's outfit from items it already owns.
---
Run `claude-bucks inventory` to see what you own, then equip a different look with `claude-bucks equip <item-id>` (or clear a slot with `claude-bucks unequip <slot>`). Show the user your new avatar line.

Slots: aura, head, face, companion, title.

$ARGUMENTS
